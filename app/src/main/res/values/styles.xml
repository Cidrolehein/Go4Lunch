<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="drawerArrowStyle">@style/DrawerHamburgerStyle</item>
    </style>

    <!-- drawer Arrow color -->
    <style name="DrawerHamburgerStyle" parent="@style/Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/white</item>
    </style>

    <!-- Connection application theme. -->
    <style name="ConnexionTheme" parent="Widget.AppCompat.ActionBar">
        <!-- Customize your theme here. -->
        <item name="android:background">@color/grey</item>
    </style>

    <!-- Generic Button style -->
    <style name="AppTheme.Generic.ButtonStyle" parent="Widget.AppCompat.Button.Colored">
        <item name="android:textColor">@color/text_button</item>
        <item name="android:drawablePadding">10dp</item>
        <item name="android:paddingEnd">20dp</item>
        <item name="android:paddingStart">10dp</item>
    </style>

    <!-- Firebase connection style -->
    <style name="FirebaseConnection" parent="AppTheme">
        <item name="actionBarStyle">@style/ConnexionTheme</item>
        <item name="android:windowBackground">@drawable/go4lunch_connection_bg</item>
        <item name= "android:windowFullscreen">true</item>
        <item name="android:scaleType">centerCrop</item>
        <item name="buttonStyle">@style/AppTheme.Login.ButtonStyle</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorControlNormal">@color/colorPrimary</item>
        <item name="colorControlActivated">@color/colorPrimaryDark</item>
        <item name="colorControlHighlight">@color/colorAccent</item>
    </style>

    <!-- Login Button style -->
    <style name="AppTheme.Login.ButtonStyle" parent="Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">@color/colorPrimary</item>
        <item name="android:textColor">@color/text_button</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:drawablePadding">20dp</item>
        <item name="android:paddingEnd">70dp</item>
        <item name="android:paddingStart">20dp</item>
    </style>

</resources>
